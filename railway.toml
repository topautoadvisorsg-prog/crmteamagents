[build]
builder = "NIXPACKS"
buildCommand = "npm install && cd ui && npm install && npm run build && cd .."

[deploy]
startCommand = "npm start"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
